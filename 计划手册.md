# MBookTI 项目计划手册

> 版本：v1.0 | 最后更新：2026-07-30
> 技术栈：Vue 3 + FastAPI + MySQL

---

## 一、项目总览

### 1.1 项目定位

AI 驱动的 MBTI 人格类型书籍推荐平台，帮助用户根据自己的人格特质发现适合自己的书籍。

### 1.2 核心理念

```
用户选择 MBTI 类型 → AI 生成针对性推荐 → 用户阅读推荐理由 → 互动（评论/点赞）
```

### 1.3 阶段路线图

```
Phase 1 ──── 核心推荐功能 ──── 2周
│  ├─ FastAPI 项目骨架
│  ├─ 数据库模型（用户系统暂不含）
│  ├─ 16 种 MBTI 类型数据
│  ├─ AI 推荐引擎（OpenAI API）
│  ├─ 核心 API（类型列表、推荐获取、AI 生成）
│  └─ 基础前端页面
│
Phase 2 ──── 用户系统 ──── 1周
│  ├─ 注册/登录（JWT）
│  ├─ 个人中心
│  ├─ 浏览历史
│  └─ 头像/昵称
│
Phase 3 ──── 评论社交 ──── 1-2周
│  ├─ 评论区（发表/回复/删除）
│  ├─ 点赞系统
│  ├─ 敏感词过滤
│  └─ 用户协议 + 隐私政策
│
Phase 4 ──── 运营增强 ──── 按需
   ├─ 管理员后台
   ├─ 数据统计
   ├─ 推荐人工编辑
   ├─ 书单分享卡片
   └─ 更多社交互动
```

### 1.4 关键决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 后端框架 | FastAPI | 已选定，异步支持好，自动 API 文档 |
| 前端框架 | Vue 3 + Vite + Pinia | 已选定，生态成熟 |
| 数据库 | MySQL | 本地已装好，够用 |
| 认证方式 | JWT（自建） | 前端分离架构，无状态 |
| AI 模型 | GPT-4o-mini / DeepSeek | 性价比优先，推荐场景不需要最强模型 |
| 部署策略 | 分阶段上线 | 快速验证核心价值 |
| 评论功能 | Phase 3 引入 | 避免拖延核心推荐上线 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                      用户（浏览器）                        │
└──────────────┬──────────────────────────┬───────────────┘
               │                          │
         Vue 3 SPA                   移动端（后续）
               │                          │
               └──────────┬───────────────┘
                          │ HTTPS
                          ▼
               ┌─────────────────────┐
               │   Nginx / Caddy      │ ← 反向代理 + SSL 终止
               └─────────┬───────────┘
                         │
          ┌──────────────┴──────────────┐
          │         FastAPI             │
          │  ┌──────────────────────┐   │
          │  │  CORS + Middleware   │   │
          │  ├──────────────────────┤   │
          │  │  Routers:            │   │
          │  │  ├─ /mbti-types      │   │
          │  │  ├─ /recommendations │   │
          │  │  ├─ /books           │   │
          │  │  ├─ /auth (P2)       │   │
          │  │  └─ /comments (P3)   │   │
          │  ├──────────────────────┤   │
          │  │  Services:           │   │
          │  │  ├─ ai_recommender   │   │
          │  │  └─ book_service     │   │
          │  └──────────────────────┘   │
          └──────────┬──────────────────┘
                     │
                     ▼
           ┌──────────────────────┐
           │       MySQL          │
          │  ┌────────────────┐  │
          │  │ mbti_types     │  │
          │  │ books          │  │
          │  │ recommendations│  │
          │  │ users     (P2) │  │
          │  │ comments  (P3) │  │
          │  └────────────────┘  │
          └──────────────────────┘
```

### 2.2 技术选型明细

| 层级 | 技术 | 版本要求 | 备注 |
|------|------|---------|------|
| 前端框架 | Vue 3 | ^3.4 | Composition API |
| 构建工具 | Vite | ^5.0 | 快速 HMR |
| 状态管理 | Pinia | ^2.0 | |
| 路由 | Vue Router | ^4.0 | |
| HTTP | Axios | ^1.0 | |
| 样式 | Tailwind CSS | ^3.4 | |
| 后端框架 | FastAPI | ^0.111 | Python 3.12+ |
| ORM | SQLAlchemy | ^2.0 | 异步模式 |
| 数据库迁移 | Alembic | ^1.13 | |
| 数据校验 | Pydantic | ^2.0 | |
| 认证 | python-jose + passlib | — | Phase 2 引入 |
| AI SDK | openai | ^1.0 | |
| 数据库 | MySQL | 8.0+ | 异步驱动 asyncmy |

### 2.3 目录结构（完整）

```
MBookTI/
├── backend/
│   ├── alembic/
│   │   ├── versions/
│   │   ├── env.py
│   │   └── script.py.mako
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                    # FastAPI 应用入口
│   │   ├── config.py                  # 环境变量配置
│   │   ├── database.py                # 数据库连接与会话
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── base.py                # 声明性基类
│   │   │   ├── mbti_type.py
│   │   │   ├── book.py
│   │   │   ├── user.py           (P2)
│   │   │   └── comment.py        (P3)
│   │   ├── schemas/
│   │   │   ├── __init__.py
│   │   │   ├── mbti_type.py
│   │   │   ├── book.py
│   │   │   ├── user.py           (P2)
│   │   │   └── comment.py        (P3)
│   │   ├── routers/
│   │   │   ├── __init__.py
│   │   │   ├── mbti_types.py
│   │   │   ├── recommendations.py
│   │   │   ├── books.py
│   │   │   ├── auth.py           (P2)
│   │   │   └── comments.py       (P3)
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── ai_recommender.py
│   │   │   ├── book_service.py
│   │   │   ├── auth_service.py   (P2)
│   │   │   └── comment_service.py(P3)
│   │   ├── core/
│   │   │   ├── __init__.py
│   │   │   ├── deps.py               # 依赖注入（get_db等）
│   │   │   ├── security.py      (P2) # JWT 加解密
│   │   │   └── exceptions.py         # 全局异常处理
│   │   └── seed/
│   │       ├── __init__.py
│   │       └── mbti_types.py          # 16种类型初始数据
│   ├── tests/                    (后续)
│   ├── alembic.ini
│   ├── .env
│   ├── .gitignore
│   ├── pyproject.toml
│   └── requirements.txt
│
├── frontend/                          # Phase 1 同步或后续创建
│   ├── public/
│   ├── src/
│   │   ├── main.ts
│   │   ├── App.vue
│   │   ├── router/index.ts
│   │   ├── stores/               (Pinia)
│   │   ├── views/
│   │   │   ├── HomePage.vue          # 首页（选择 MBTI）
│   │   │   ├── RecommendationPage.vue # 推荐结果页
│   │   │   ├── BookDetailPage.vue     # 书籍详情
│   │   │   ├── LoginPage.vue    (P2)
│   │   │   └── ProfilePage.vue  (P2)
│   │   ├── components/
│   │   │   ├── MbtiSelector.vue       # MBTI 选择器
│   │   │   ├── BookCard.vue           # 书籍卡片
│   │   │   └── CommentSection.vue(P3)
│   │   ├── composables/
│   │   │   └── useApi.ts
│   │   └── api/
│   │       └── index.ts              # Axios 实例 + API 函数
│   ├── index.html
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   └── postcss.config.js
│
├── docker-compose.yml                 # 可选，本地 MySQL 容器
├── 计划手册.md                        # 本文件
└── README.md
```

---

## 三、数据库设计

### 3.1 ER 图（文字版）

```
┌──────────────┐       ┌──────────────────┐       ┌──────────────┐
│   mbti_types │       │  recommendations │       │    books     │
├──────────────┤       ├──────────────────┤       ├──────────────┤
│ id (PK)      │──1:N──│ mbti_type_id (FK)│──N:1──│ id (PK)      │
│ code         │       │ book_id (FK)     │       │ title        │
│ name         │       │ reasoning        │       │ author       │
│ name_en      │       │ relevance_score  │       │ isbn (UQ)    │
│ description  │       │ is_ai_generated  │       │ cover_url    │
│ traits (JSON)│       │ likes_count      │       │ description  │
│ created_at   │       │ created_at       │       │ genre        │
└──────────────┘       └──────────────────┘       └──────────────┘
     │                           │
     │ (Phase 2) FK              │ (Phase 3) 1:N
     ▼                           ▼
┌──────────────┐          ┌──────────────┐
│    users     │          │   comments   │
├──────────────┤          ├──────────────┤
│ id (PK)      │──1:N────▶│ id (PK)      │
│ email        │          │ user_id (FK) │
│ username     │          │ content      │
│ ...          │          │ parent_id    │← 自引用
│ mbti_type_id │          │ is_hidden    │
│ created_at   │          │ created_at   │
└──────────────┘          └──────────────┘
```

### 3.2 表结构说明

#### mbti_types — MBTI 类型字典表

```sql
CREATE TABLE mbti_types (
    id          SMALLINT    NOT NULL AUTO_INCREMENT PRIMARY KEY,
    code        VARCHAR(4)  NOT NULL UNIQUE,
    name        VARCHAR(50) NOT NULL,
    name_en     VARCHAR(50) NOT NULL,
    description TEXT        NOT NULL,
    traits      JSON        NOT NULL DEFAULT ('{}'),
    created_at  DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
CREATE INDEX idx_mbti_types_code ON mbti_types(code);
```

#### books — 书籍库

```sql
CREATE TABLE books (
    id          INT          NOT NULL AUTO_INCREMENT PRIMARY KEY,
    title       VARCHAR(255) NOT NULL,
    author      VARCHAR(255) NOT NULL,
    isbn        VARCHAR(20)  UNIQUE,
    cover_url   TEXT,
    description TEXT,
    genre       VARCHAR(100),
    language    VARCHAR(10)  NOT NULL DEFAULT 'zh',
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
CREATE INDEX idx_books_title ON books(title);
```

#### recommendations — 推荐关系

```sql
CREATE TABLE recommendations (
    id              INT       NOT NULL AUTO_INCREMENT PRIMARY KEY,
    mbti_type_id    SMALLINT  NOT NULL,
    book_id         INT       NOT NULL,
    reasoning       TEXT      NOT NULL,
    relevance_score SMALLINT  NOT NULL DEFAULT 5,
    is_ai_generated BOOLEAN   NOT NULL DEFAULT TRUE,
    likes_count     INT       NOT NULL DEFAULT 0,
    created_at      DATETIME  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(mbti_type_id, book_id),
    FOREIGN KEY (mbti_type_id) REFERENCES mbti_types(id),
    FOREIGN KEY (book_id) REFERENCES books(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
CREATE INDEX idx_recommendations_mbti ON recommendations(mbti_type_id);
```

### 3.3 Phase 2/3 扩展表

```sql
-- Phase 2: 用户表
CREATE TABLE users (
    id              INT          NOT NULL AUTO_INCREMENT PRIMARY KEY,
    email           VARCHAR(255) NOT NULL UNIQUE,
    username        VARCHAR(50)  NOT NULL UNIQUE,
    password_hash   VARCHAR(255) NOT NULL,
    avatar_url      TEXT,
    mbti_type_id    SMALLINT,
    is_admin        BOOLEAN      NOT NULL DEFAULT FALSE,
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (mbti_type_id) REFERENCES mbti_types(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Phase 3: 评论表
CREATE TABLE comments (
    id                INT       NOT NULL AUTO_INCREMENT PRIMARY KEY,
    user_id           INT       NOT NULL,
    recommendation_id INT       NOT NULL,
    parent_id         INT,
    content           TEXT      NOT NULL,
    likes_count       INT       NOT NULL DEFAULT 0,
    is_edited         BOOLEAN   NOT NULL DEFAULT FALSE,
    is_hidden         BOOLEAN   NOT NULL DEFAULT FALSE,
    created_at        DATETIME  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (recommendation_id) REFERENCES recommendations(id),
    FOREIGN KEY (parent_id) REFERENCES comments(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE comment_likes (
    user_id    INT      NOT NULL,
    comment_id INT      NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (user_id, comment_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (comment_id) REFERENCES comments(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 四、API 设计

### 4.1 Phase 1 API

#### `GET /api/v1/mbti-types`

返回全部 16 种 MBTI 类型列表。

```json
{
  "data": [
    {
      "code": "INTJ",
      "name": "提倡者",
      "name_en": "Advocate",
      "description": "...",
      "traits": ["战略思维", "独立", "理性"]
    }
  ],
  "total": 16
}
```

#### `GET /api/v1/mbti-types/{code}`

返回单个 MBTI 类型详情（含已有推荐数量统计）。

#### `GET /api/v1/recommendations/mbti/{code}`

获取某类型的推荐书单。若尚未生成过 AI 推荐，返回空数组。

```json
{
  "data": [
    {
      "id": "uuid",
      "book": {
        "title": "书名",
        "author": "作者",
        "cover_url": "...",
        "description": "...",
        "genre": "类别"
      },
      "reasoning": "AI 推荐理由",
      "relevance_score": 9,
      "is_ai_generated": true,
      "likes_count": 0,
      "created_at": "2026-07-30T12:00:00Z"
    }
  ],
  "mbti_type": {
    "code": "INTJ",
    "name": "提倡者"
  }
}
```

#### `POST /api/v1/recommendations/ai-generate`

触发 AI 生成推荐。

请求：

```json
{
  "mbti_code": "INTJ",
  "count": 5
}
```

响应（同 `GET` 结构）：

```json
{
  "data": [...],
  "source": "ai_generated",
  "cached": false
}
```

> 注意：如果该类型 24 小时内已生成过，返回 409 Conflict，提示直接读取已有推荐。

#### `GET /api/v1/books/{id}`

单本书籍详情。

### 4.2 Phase 2/3 API（预留）

| Phase | Method | Path | 说明 |
|-------|--------|------|------|
| P2 | `POST` | `/api/v1/auth/register` | 注册 |
| P2 | `POST` | `/api/v1/auth/login` | 登录，返回 JWT |
| P2 | `GET` | `/api/v1/users/me` | 个人信息 |
| P2 | `PATCH` | `/api/v1/users/me` | 更新 MBTI 等 |
| P3 | `GET` | `/api/v1/recommendations/{id}/comments` | 评论列表 |
| P3 | `POST` | `/api/v1/recommendations/{id}/comments` | 发表评论 |
| P3 | `DELETE` | `/api/v1/comments/{id}` | 删除评论 |
| P3 | `POST` | `/api/v1/comments/{id}/like` | 点赞/取消 |

### 4.3 统一响应格式

所有 API 返回统一格式：

```json
{
  "data": {},           // 实际数据
  "error": null,        // 错误时为错误信息
  "meta": {             // 可选，分页等
    "total": 16,
    "page": 1,
    "per_page": 20
  }
}
```

---

## 五、AI 推荐引擎设计

### 5.1 工作流程

```
用户选择 MBTI 类型
       │
       ▼
  GET /recommendations/mbti/{code}
       │
       ├── 已有推荐 → 返回
       │
       └── 无推荐 → 前端显示"生成推荐"按钮
                        │
                        ▼
                 POST /recommendations/ai-generate
                        │
                        ▼
              ┌─────────────────────┐
              │  构造 Prompt        │
              │  (含MBTI特质、偏好)  │
              └─────────┬───────────┘
                        │
              ┌─────────▼───────────┐
              │  调用 LLM API       │
              │  (JSON mode)        │
              └─────────┬───────────┘
                        │
              ┌─────────▼───────────┐
              │  解析结果            │
              │  去重（是否已在DB）  │
              └─────────┬───────────┘
                        │
              ┌─────────▼───────────┐
              │  写入数据库          │
              │  books + recs       │
              └─────────┬───────────┘
                        │
                        ▼
                 返回推荐结果
```

### 5.2 Prompt 设计（核心）

```python
MBTI_RECOMMEND_PROMPT = """你是一位专业的阅读顾问。用户的MBTI类型是 {mbti_code}。

## MBTI 类型特质
{traits}

## 阅读偏好
{reading_style}

## 推荐要求
1. 推荐 {count} 本最适合该类型的书籍
2. 优先推荐中文译本或华语作者的作品
3. 书籍必须真实存在，不要虚构
4. 每本书的推荐理由要结合 MBTI 特质具体说明"为什么这本书适合这个类型"

## 输出格式 (JSON)
返回 JSON 数组，每项包含：
- title: 书名
- author: 作者
- description: 内容简介（50-100字）
- reasoning: 推荐理由（80-150字，结合MBTI特质）
- genre: 类别（如：心理学、科幻、哲学等）
"""
```

### 5.3 16种 MBTI 特质数据（Prompt 用）

| 类型 | 核心特质 | 阅读偏好 |
|------|---------|---------|
| INTJ | 战略思维、独立、完美主义、理性 | 系统性、有深度的非虚构 |
| INTP | 逻辑、分析、创新、理论 | 科学、技术、哲学 |
| ENTJ | 领导力、效率、决断、目标导向 | 商业、传记、战略 |
| ENTP | 辩论、创新、灵活、好奇心 | 跨学科、科技、推理 |
| INFJ | 洞察力、理想主义、深度、利他 | 心理学、哲学、文学 |
| INFP | 理想主义、创造力、同理心、价值驱动 | 文学、诗歌、心理学 |
| ENFJ | 感染力、利他、组织、激励 | 自我提升、人际关系、传记 |
| ENFP | 热情、创造力、好奇心、社交 | 心理学、小说、创意 |
| ISTJ | 可靠、责任、传统、细致 | 历史、实用手册、纪实 |
| ISFJ | 体贴、忠诚、务实、细心 | 生活、健康、家庭 |
| ESTJ | 执行力、秩序、效率、务实 | 管理、经济、实用 |
| ESFJ | 友善、合作、尽责、传统 | 社交、健康、生活 |
| ISTP | 冷静、动手、灵活、探索 | 技术、冒险、工艺 |
| ISFP | 敏感、艺术、和谐、行动 | 艺术、旅行、摄影 |
| ESTP | 精力、冒险、务实、灵活 | 冒险、商业、运动 |
| ESFP | 热情、表现、社交、乐观 | 娱乐、艺术、旅行 |

### 5.4 缓存策略

```
AI 生成结果后写入数据库 + 标记时间戳
同类型 24 小时内不再重复调用 LLM
超过 24 小时 → 前端显示"重新生成"按钮，用户主动触发
```

---

## 六、前端设计概要

### 6.1 页面路由

| 路径 | 页面 | Phase |
|------|------|-------|
| `/` | 首页（选择 MBTI） | P1 |
| `/types/:code` | 类型详情 + 推荐列表 | P1 |
| `/books/:id` | 书籍详情 | P1 |
| `/login` | 登录页 | P2 |
| `/register` | 注册页 | P2 |
| `/profile` | 个人中心 | P2 |

### 6.2 首页交互流程

```
1. 用户进入首页
   └─ 展示 16 个 MBTI 类型卡片（4×4 网格）
      ├─ 显示类型名称、中文称呼、简短描述
      └─ 点击卡片

2. 进入类型详情页
   ├─ 顶部：类型详细描述 + 特质标签
   ├─ 中部：推荐书单
   │   ├─ 已有推荐 → 卡片列表（封面+书名+作者+推荐理由）
   │   └─ 无推荐 → "AI 生成推荐" 按钮
   └─ 底部：回到首页

3. 点击书籍卡片
   └─ 进入书籍详情页（Phase 1 为简单信息展示）
```

### 6.3 关键组件

```
MbtiSelector.vue       — 16 格 MBTI 类型选择器
MbtiTypeCard.vue       — 单个 MBTI 类型卡片
BookCard.vue           — 书籍推荐卡片
RecommendationList.vue — 推荐列表（含加载/空态）
AiGenerateButton.vue   — AI 生成触发按钮（含 loading 状态）
```

---

## 七、部署方案

### 7.1 开发环境

```yaml
# docker-compose.yml（可选，已有本地 MySQL 可跳过）
version: "3.8"
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: mbookti
      MYSQL_ROOT_PASSWORD: CHANGEME
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

volumes:
  mysql_data:
```

### 7.2 生产环境部署

```
方案：Docker + VPS (或 Railway)

┌──────────┐     ┌──────────┐     ┌──────────────────┐
│  Vercel  │     │   VPS    │     │  云数据库 RDS    │
│ (前端)   │────▶│ (后端)   │────▶│ 或自建 MySQL     │
│          │     │ Docker   │     │                  │
└──────────┘     └──────────┘     └──────────────────┘

预计每月成本:
  ├─ Vercel Hobby:     $0
  ├─ VPS (2C4G):       ~$10
  ├─ 数据库:           $0 (自建) 或 ~$10 (云RDS)
  ├─ 域名:             ~$1
  └─ LLM API:          ~$2-5 (初期调用量小)
  合计:                ~$13-25/月
```

### 7.3 环境变量

```bash
# backend/.env
DATABASE_URL=mysql+asyncmy://root:CHANGEME@localhost:3306/mbookti?charset=utf8mb4
DEEPSEEK_API_KEY=sk-xxx
DEEPSEEK_API_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
CORS_ORIGINS=http://localhost:5173,https://your-domain.com
AI_CACHE_TTL=86400
```

---

## 八、开发规范

### 8.1 代码规范

- Python: 遵循 PEP 8，使用 `ruff` 进行 lint 和格式化
- TypeScript/Vue: 使用 ESLint + Prettier
- 提交信息：`<type>(<scope>): <description>`（如 `feat(ai): add MBTI recommendation engine`）

### 8.2 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| Python 变量/函数 | snake_case | `get_recommendations()` |
| Python 类 | PascalCase | `AIRecommender` |
| TypeScript 变量/函数 | camelCase | `getRecommendations()` |
| TypeScript 类/组件 | PascalCase | `BookCard.vue` |
| 数据库表 | snake_case 复数 | `mbti_types` |
| API 路径 | kebab-case | `/api/v1/mbti-types` |

### 8.3 代码审查清单（提交前检查）

- [ ] LSP diagnostics 无 error
- [ ] 未使用 `Any`、`as any`、`@ts-ignore`
- [ ] 异常分支有错误处理（非空 catch）
- [ ] 敏感信息不硬编码（API Key 等走环境变量）
- [ ] 数据库查询有适当的 index
- [ ] 新 API 遵循统一响应格式

---

## 九、Phase 1 执行计划（当前阶段）

### 9.1 任务分解

```
Step 1: 后端项目骨架
  ├── pyproject.toml + requirements.txt
  ├── app/config.py         ← 环境变量
  ├── app/database.py       ← 数据库连接
  ├── app/models/*          ← SQLAlchemy 模型
  ├── app/main.py           ← FastAPI 入口
  └── alembic 初始化 + 首次迁移

Step 2: 种子数据
  └── app/seed/mbti_types.py  ← 16 种 MBTI 数据

Step 3: AI 推荐引擎
  └── app/services/ai_recommender.py

Step 4: API 路由
  ├── app/routers/mbti_types.py
  ├── app/routers/recommendations.py
  └── app/routers/books.py

Step 5: 基础前端
  ├── Vue 3 项目初始化
  ├── MBTI 类型选择页
  └── 推荐结果展示页

Step 6: 部署上线
  ├── Dockerfile
  ├── docker-compose.prod.yml
  └── 部署到 VPS / Railway
```

### 9.2 预计工时

| 步骤 | 工时 | 依赖 |
|------|------|------|
| Step 1: 项目骨架 | 0.5 天 | — |
| Step 2: 种子数据 | 0.5 天 | Step 1 |
| Step 3: AI 引擎 | 1 天 | Step 1 |
| Step 4: API 路由 | 1 天 | Step 1, 2, 3 |
| Step 5: 前端 | 2 天 | Step 4 |
| Step 6: 部署 | 0.5 天 | Step 4, 5 |
| **合计** | **~5.5 天** | |

### 9.3 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| LLM API 返回虚假书籍（幻觉） | 信任度受损 | Prompt 明确要求真实书籍 + prompt 中加入"不要虚构" |
| AI 调用延迟（3-5秒） | 用户体验差 | 前端加 loading 状态缓存结果，后续可考虑流式响应 |
| 数据库设计需后续扩展 | 重构成本 | Phase 1 预留 user_id 等字段，不做大改动 |
| 评论功能合规风险 | 法律问题 | Phase 3 加入敏感词过滤，用户协议明确责任 |

---

## 十、版本记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-07-30 | 初版完整计划（PostgreSQL） | Sisyphus |
| v1.1 | 2026-07-30 | 数据库切换为 MySQL，适配表结构和配置 | Sisyphus |
| | | | |
