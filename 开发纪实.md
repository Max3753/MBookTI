# MBookTI 开发纪实

> 本项目开发过程完整记录：操作顺序、问题决策、解决方案、代码逻辑。
> 用途：复盘学习。配合 git 历史（`a28519b` init 提交之后的全部改动）一起看效果最佳。

---

## 一、项目概览

| 项目 | 内容 |
|---|---|
| 名称 | MBookTI |
| 定位 | 根据 MBTI 人格类型做针对性书籍推荐 |
| 架构 | 前后端分离（Vue 3 + FastAPI） |
| 数据库 | MySQL（SQLAlchemy async + PyMySQL） |
| 认证 | JWT（python-jose） |
| 密码 | bcrypt（passlib） |
| AI | DeepSeek API（OpenAI 兼容接口） |

### 目录结构（涉及本次开发的部分）

```
backend/
├── app/
│   ├── main.py              # 应用入口：路由注册、CORS、异常处理
│   ├── config.py            # 环境配置（含 JWT 配置）
│   ├── database.py          # 异步数据库引擎
│   ├── models/              # SQLAlchemy ORM 模型
│   ├── schemas/             # Pydantic 请求/响应模型（本次新增）
│   ├── routers/             # 路由（books/mbti_types/recommendations/auth/comments）
│   ├── auth/                # JWT 认证模块（本次新增）
│   ├── seed/                # MBTI 类型种子数据
│   └── services/            # AI 推荐服务
└── .env                     # 敏感配置（已 gitignore）
frontend/
└── src/
    ├── api/                 # axios 实例 + 接口封装
    ├── composables/         # useTheme / useI18n / useAuth（useAuth 本次新增）
    ├── router/              # 路由 + 守卫
    └── views/               # HomePage / TypeDetailPage / LoginPage / RegisterPage
```

---

## 二、开发时间线总览

| 阶段 | 内容 | 产出 |
|---|---|---|
| 一 | 基础修复 | 主题切换、i18n 清理、安全审查、首次推送 GitHub |
| 二 | 后端 Schemas 重构 | 统一响应格式 + 响应校验 |
| 三 | JWT 用户认证 | 注册/登录接口 |
| 四 | 评论系统 | 评论 CRUD + 点赞（后端） |
| 五 | 前端功能 + 打磨 | 登录/注册页、评论区、过渡动画、返回按钮 |
| 六 | 联调调试 | 7 个 bug 的发现与修复 |

---

## 三、阶段一：基础修复

### 3.1 主题切换不生效

**现象**：点击主题按钮，页面不变色。

**排查过程**：
1. 检查 `useTheme.ts` —— 逻辑是切换 `html` 标签的 `dark` class
2. 检查 `style.css` —— 发现是 Tailwind v4

**根因**：Tailwind v4 默认不再自动响应 `.dark` 类，需要显式声明 dark 变体。

**修复**：`frontend/src/style.css` 添加：
```css
@custom-variant dark (&:where(.dark, .dark *));
```

**要点**：Tailwind v4 和 v3 的暗色模式机制不同。v3 内置 `.dark` 支持，v4 需要手动声明 `@custom-variant`。

### 3.2 删除全局语言切换

**决策**：用户要求删除语言切换功能，保留中文。

**改动**：
- `useI18n.ts`：从"响应式语言状态 + 切换函数"简化为"纯静态对象 + 导出 `t`"
- 各页面：从 `const { t } = useI18n()` 改为 `import { t } from '../composables/useI18n'`

**学习点**：不需要响应式的常量不要放进 `ref`，直接导出普通对象即可，减少心智负担。

### 3.3 安全审查

**操作**：
1. 根目录创建 `.gitignore`：加入 `.env`、`.vscode/`、`node_modules`、`dist`、`__pycache__` 等
2. `backend/.env` 确保被忽略（数据库密码、API Key 不泄漏）
3. `config.py` 去掉数据库 URL 的硬编码默认值
4. `计划手册.md` 中占位密码改为 `CHANGEME`
5. 用 `git add -n --all` 预览确认无敏感文件被追踪

**学习点**：推送到公开仓库前，安全自查顺序：`.gitignore` → 全库搜索密钥 → 预览暂存 → 推送。

### 3.4 API baseURL 环境化

新建 `frontend/src/api/config.ts`：
```ts
const apiConfig = {
    baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000/api/v1',
    timeout: 10000,
}
```
开发/生产环境通过 `.env` 里的 `VITE_API_BASE_URL` 区分，无需改代码。

### 3.5 首次推送 GitHub

```
git push -u origin master   # 推送到 Max3753/MBookTI
```

---

## 四、阶段二：后端 Schemas 重构

### 4.1 为什么加 Schemas 层

之前 router 直接返回 `{"data": book}` 裸 dict，FastAPI 不知道响应结构。加了 Pydantic Schema 后：
- 接口有明确的请求/响应结构定义（类似 TS 的 interface）
- FastAPI 自动做**响应校验**——字段缺失/类型错会直接报错，把 bug 拦在出口

### 4.2 统一响应格式（schemas/common.py）

```python
from typing import Generic, TypeVar
from pydantic import BaseModel

T = TypeVar("T")

class ApiResponse(BaseModel, Generic[T]):
    """单条数据响应"""
    data: T
    message: str = "success"

class ApiListResponse(BaseModel, Generic[T]):
    """列表响应"""
    data: list[T]
    total: int
    message: str = "success"
```

**学习点：Pydantic 泛型**。`Generic[T]` 让 `ApiResponse` 成为"模板容器"，使用时指定具体类型：
```python
response_model=ApiResponse[BookResponse]      # 单条书籍
response_model=ApiListResponse[MbtiTypeResponse]  # 类型列表
```
响应体统一为 `{data: ..., message: "success"}`，前端解包逻辑一致。

### 4.3 各业务 Schema

```
schemas/
├── mbti_type.py       # MbtiTypeResponse
├── book.py            # BookResponse
├── recommendation.py  # RecommendationResponse / AIGenerateRequest / AIGenerateBook
├── user.py            # UserResponse / TokenResponse / 注册登录请求
└── comment.py         # CommentResponse / CommentCreateRequest
```

关键点：所有响应类都有 `model_config = {"from_attributes": True}` —— 允许直接从 SQLAlchemy ORM 对象构造 Pydantic 模型（ORM → Pydantic 双向转换）。

### 4.4 Router 改造模式（三处一致）

以 `books.py` 为例：
```python
# 之前：裸 dict
@router.get("/{book_id}")
async def get_book(...):
    return {"data": book}

# 之后：声明 response_model + Schema 包装
@router.get("/{book_id}", response_model=ApiResponse[BookResponse])
async def get_book(...):
    return ApiResponse(data=book)
```

**学习点：`response_model=` 是 FastAPI 的核心机制**。接口返回前按声明结构校验/序列化，字段缺失或类型错误直接 500。之后遇到的 `nane_en` 拼写错误、`traits` 类型不匹配，就是靠它暴露的。

### 4.5 一处数据格式修正

`recommendations.py` 的 `get_recommendations`：
- `created_at` 由直接传 datetime 对象 → `rec.created_at.isoformat()`（ISO 字符串，避免序列化问题）
- 返回结构改为 `{items: [...], mbti_type: {...}}`（这个结构变化后面引发了前端 bug，见阶段六）

---

## 五、阶段三：JWT 用户认证

### 5.1 认证原理一句话

JWT = 带签名的自包含令牌。服务端用 `SECRET_KEY` 签发，客户端持有，每次请求带上，服务端用同一个 key 验证签名。签名被篡改则验证失败。

### 5.2 模块结构

```
backend/app/auth/
├── password.py    # 密码哈希（bcrypt）
├── jwt.py         # Token 生成/验证（python-jose）
└── deps.py        # 依赖注入：从请求头提取并验证用户
```

### 5.3 password.py —— 单向哈希

```python
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:      # 注册时用
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:  # 登录时用
    return pwd_context.verify(plain, hashed)
```

**学习点**：
- 哈希**不可逆**，数据库永远只存 `password_hash`
- 即使数据库被拖走，也还原不出明文密码
- `verify` 比较时做常数时间比较，防时序攻击

### 5.4 jwt.py —— 签发与验证

```python
from jose import jwt
from jose.exceptions import JWTError

SECRET_KEY = settings.jwt_secret_key
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7 天

def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def decode_access_token(token: str) -> Optional[dict]:
    try:
        return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    except JWTError:
        return None
```

**学习点**：
- payload 里塞了 `exp`（过期时间），验证时自动检查
- `timezone.utc` —— 时间戳统一用 UTC，避免时区混乱
- 解码失败返回 `None` 而非抛异常，调用方好处理

### 5.5 deps.py —— 依赖注入（FastAPI 精华）

```python
bearer_scheme = HTTPBearer()

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(bearer_scheme),
    session: AsyncSession = Depends(get_db),
) -> User:
    payload = decode_access_token(credentials.credentials)
    if payload is None:
        raise HTTPException(401, "无效的 Token")
    user_id = payload.get("sub")
    ...
    result = await session.execute(select(User).where(User.id == int(user_id)))
    return result.scalar_one_or_none()
```

**学习点**：任何接口想"要求登录"，只需：
```python
async def create_comment(..., current_user: User = Depends(get_current_user)):
```
FastAPI 自动完成：取请求头 → 解析 token → 查库 → 给你用户对象。**不需要在每个接口重复写验证代码**。这就是依赖注入的威力。

### 5.6 routers/auth.py —— 注册/登录

```python
@router.post("/register", response_model=ApiResponse[TokenResponse])
async def register(req: UserRegisterRequest, session: AsyncSession = Depends(get_db)):
    # 1. 查重（用户名或邮箱已存在 → 400）
    # 2. User(username=..., email=..., password_hash=hash_password(req.password))
    # 3. commit + refresh
    # 4. token = create_access_token({"sub": str(user.id), "username": user.username})
    # 5. 返回 ApiResponse(data=TokenResponse(access_token=token, user=...))

@router.post("/login", response_model=ApiResponse[TokenResponse])
async def login(req: UserLoginRequest, session: AsyncSession = Depends(get_db)):
    # 1. 按用户名查用户
    # 2. verify_password(req.password, user.password_hash) 失败 → 401
    # 3. 签发 token 返回
```

设计决策：**注册成功直接返回 token**（相当于注册即登录），省去前端二次跳转。

---

## 六、阶段四：评论系统（后端）

### 6.1 接口设计

| 接口 | 鉴权 | 说明 |
|---|---|---|
| `POST /api/v1/comments` | 必须登录 | 创建评论 |
| `GET /api/v1/comments/recommendation/{id}` | 公开 | 获取某推荐的评论列表 |
| `POST /api/v1/comments/{id}/like` | 必须登录 | 点赞/取消点赞（toggle） |

### 6.2 点赞的 toggle 逻辑（设计亮点）

```python
existing = await session.execute(
    select(CommentLike).where(
        CommentLike.user_id == current_user.id,
        CommentLike.comment_id == comment_id,
    )
)
like = existing.scalar_one_or_none()

if like:
    await session.delete(like)
    comment.likes_count = max(0, comment.likes_count - 1)
    return ApiResponse(data={"liked": False, "likes_count": comment.likes_count})
else:
    session.add(CommentLike(user_id=..., comment_id=...))
    comment.likes_count += 1
    return ApiResponse(data={"liked": True, "likes_count": comment.likes_count})
```

**学习点**：一个接口实现"赞/取消赞"两种行为 —— 查 `CommentLike` 表有没有记录，有就删（取消），没有就加（点赞）。`comment_likes` 是联合主键表 `(user_id, comment_id)`，天然防重复点赞。前端按钮无需维护状态，每次点击都调这个接口即可。

### 6.3 评论列表联查

```python
select(Comment, User).join(User, Comment.user_id == User.id).where(...)
```
因为评论需要显示用户名，而 `Comment` 表只存 `user_id`，所以 join `User` 表取 `username`。

---

## 七、阶段五：前端功能 + 打磨

### 7.1 useAuth.ts —— 模块级单例状态

```ts
// 模块顶部定义 —— 整个应用共享同一份状态（不是每组件一份）
const token = ref(localStorage.getItem('token') || '')
let _user: any = null
try { _user = JSON.parse(localStorage.getItem('user') || 'null') } catch {}

if (token.value) {
    api.defaults.headers.common['Authorization'] = `Bearer ${token.value}`  // 恢复请求头
}

export function useAuth() {
    const isLoggedIn = computed(() => !!token.value)

    async function login(username: string, password: string) {
        const res = await api.post('/auth/login', { username, password })
        const d = res.data.data                       // 解包 {data: {access_token, user}}
        token.value = d.access_token
        currentUser.value = d.user
        localStorage.setItem('token', d.access_token) // 持久化
        api.defaults.headers.common['Authorization'] = `Bearer ${d.access_token}`  // 注入 axios
    }
    // register / logout 同理
    return { token, user: currentUser, isLoggedIn, login, register, logout }
}
```

**学习点**：
- **模块级单例**：`ref` 定义在 `useAuth()` 函数外面，所有组件调用 `useAuth()` 拿到的是同一份状态。这是 Vue composable 的两种形态之一（另一种是函数内定义 = 每组件独立）
- **localStorage 持久化**：刷新页面后 token 还在，模块加载时自动恢复登录态 + 恢复 axios 请求头
- **axios 全局请求头**：登录后 `Authorization: Bearer xxx` 自动附加到所有请求，后端 `get_current_user` 才能识别身份

### 7.2 路由守卫（router/index.ts）

```ts
router.beforeEach((to, from, next) => {
    const { isLoggedIn } = useAuth()
    if ((to.path === '/login' || to.path === '/register') && isLoggedIn.value) {
        next('/')   // 已登录还访问登录页 → 踢回首页
    } else {
        next()
    }
})
```
**学习点**：路由守卫在每次导航前执行，适合做全局的访问控制。

### 7.3 App.vue —— 头部 + 过渡动画

头部按登录态切换：
```html
<template v-if="isLoggedIn">
    <span>{{ user?.username }}</span>
    <button @click="logout">退出</button>
</template>
<template v-else>
    <router-link to="/login">登录</router-link>
    <router-link to="/register">注册</router-link>
</template>
```

页面过渡动画（这是阶段六 bug 的重灾区，先记住写法）：
```html
<router-view v-slot="{ Component }">
    <Transition name="fade" mode="out-in">
        <component :is="Component" :key="$route.fullPath" />
    </Transition>
</router-view>
```

### 7.4 TypeDetailPage —— 评论区

依赖 `useAuth().isLoggedIn` 三态切换：未登录 → "去登录"提示；已登录 → 评论表单；有数据 → 评论列表 + 点赞按钮（`activeRecId` 取推荐列表第一个的 id，评论区挂在推荐书单下面）。

### 7.5 返回按钮

- 需求：所有二级页面（类型详情/登录/注册）加返回首页按钮
- 教训：第一版放进了 `v-else` 分支里，API 报错时按钮不显示（见阶段六 Bug 5）
- 修正：移到模板最外层，任何状态都可见

### 7.6 useI18n 新增评论文案

`t` 对象新增 6 条：`comments_title / comments_empty / comments_login / comments_placeholder / comments_submit / comments_submitting`。

---

## 八、阶段六：联调调试 —— 7 个 Bug 全记录

> 这一节是复盘重点。每个 bug 按「现象 → 排查 → 根因 → 修复」记录，总结排查方法论。

### Bug 1：后端启动失败 —— `No module named 'passlib'`

**现象**：`uvicorn app.main:app` 启动报 `ModuleNotFoundError: No module named 'passlib'`。

**根因**：auth 模块用了 passlib，但依赖没装进 venv。

**修复**：
```
uv pip install passlib pyjwt bcrypt
```

**排查方法论**：新模块（auth）引入新依赖 → 先装依赖再跑。启动报错先看 traceback 最底部一行。

### Bug 2：后端启动失败 —— `No module named 'jose'`

**现象**：装上 passlib 后，`jwt.py` 报 `No module named 'jose'`。

**根因**：`jwt.py` 用的是 `python-jose` 库，而我装了 `pyjwt` —— **两个不同的库**。

**修复**：
```
uv pip install "python-jose[cryptography]"
```

**学习点**：JWT 生态有两个主流库：`pyjwt`（`import jwt`）和 `python-jose`（`from jose import jwt`）。**先看代码怎么 import，再装对应的包**，别想当然。

### Bug 3：MBTI 类型接口 500 —— 32 个 ResponseValidationError

**现象**：`GET /api/v1/mbti_types` 报 32 个校验错误：
```
{'type': 'missing', 'loc': ('response', 'data', 0, 'nane_en'), 'msg': 'Field required'}
{'type': 'dict_type', 'loc': ('response', 'data', 0, 'traits'), 'msg': 'Input should be a valid dictionary', 'input': ['战略思维', '独立', '理性', '完美主义']}
```

**排查**：对照 `schemas/mbti_type.py` 和 `models/mbti_type.py` 逐字段比对。

**根因**（两个）：
1. **拼写错误**：Schema 里 `nane_en`，模型里是 `name_en`（Pydantic 校验时找不到字段）
2. **类型不匹配**：Schema 声明 `traits: dict`，但数据库实际存的是 JSON 数组 `["战略思维", ...]`（list）

**修复**（schemas/mbti_type.py）：
```python
name_en: str          # 之前是 nane_en
traits: list[str]     # 之前是 dict
```

**学习点**：
- Schema 是"契约"，必须和模型/数据实际形态一致
- `response_model` 校验救了一命 —— 如果没有这层，脏数据会直接发给前端，前端再"空白"就更难查
- 排查这类问题：把报错里的 `loc`（字段路径）和 `input`（实际值）截图/复制下来，逐个对照

### Bug 4：类型详情页空白 —— 前端解包路径错误

**现象**：点击 MBTI 类型进入详情页，人格卡片和推荐列表都不显示（后端其实已 200 OK）。

**排查**：后端日志显示 `GET /api/v1/recommendations/mbti/INTJ 200 OK`，说明数据已返回，问题在前端解包。

**根因**：后端 `get_recommendations` 返回结构是 `{data: {items: [...], mbti_type: {...}}, message: "success"}`，前端却写成：
```ts
recommendations.value = recRes.data || []   // recRes.data 是整个对象 {items, mbti_type}！
```
`v-for` 遍历一个对象 → 渲染空白。

**修复**（TypeDetailPage.vue）：
```ts
recommendations.value = recRes.data?.items || []
```

**学习点**：前后端字段结构必须对齐。**解包时先确认后端返回的 JSON 长什么样**（F12 Network 看响应体），再写提取代码。这是前后端联调最常见的坑。

### Bug 5：返回按钮不显示

**现象**：加了返回按钮，页面上看不到。

**根因**：按钮放在了模板 `v-else` 分支内。API 正常时能看到，但一旦走 `v-else-if="error"` 分支（报错状态），按钮就不渲染。

**修复**：把返回按钮移到所有 `v-if/v-else-if/v-else` **分支之外**，任何状态下都可见。

**学习点**：全局可见的 UI（返回/导航）不能放进条件渲染分支里。

### Bug 6：导航连锁空白 —— Transition 要求单根元素 + 缺 :key

**现象**：从详情页点 MBookTI 返回首页，首页空白；再进其他页面也空白。刷新才恢复。控制台警告：
```
[Vue warn]: Component inside <Transition> renders non-element root node that cannot be animated.
```

**排查**：警告信息直接指明了问题 —— "renders non-element root node"。

**根因**（两个叠加）：
1. **`TypeDetailPage` 有多个根节点**（返回按钮 + 3 个 `v-if/v-else` 块 = fragment）。`<Transition>` 只能动画**单个根元素**，多根节点时过渡失败 → 组件渲染中断 → 后续导航连锁空白
2. **`<component :is>` 缺 `:key`**：路由切换时组件可能被复用，`onMounted` 不重新执行，数据不刷新

**修复**（两处）：
```html
<!-- App.vue：加 :key 强制每次路由重建组件 -->
<component :is="Component" :key="$route.fullPath" />

<!-- TypeDetailPage.vue：整个 template 包一层 <div>，保证单根元素 -->
<template>
    <div>
        <button>返回</button>
        <div v-if="loading">...</div>
        <div v-else-if="error">...</div>
        <div v-else>...</div>
    </div>
</template>
```

**学习点**：
- Vue 3 支持多根组件（fragment），但 `<Transition>` **只支持单根元素**，这是硬性限制
- 路由切换数据不刷新 → 先查有没有 `:key`
- Vue 的 warning 信息通常直接指到根因，别跳过警告

### Bug 7：注册失败 —— CORS 报错，根因却是 bcrypt

**现象**：注册请求报 CORS 错误：
```
Access to XMLHttpRequest at 'http://localhost:5000/api/v1/auth/register'
from origin 'http://localhost:5173' has been blocked by CORS policy:
No 'Access-Control-Allow-Origin' header is present.
```
后面跟着 `POST ... 500 (Internal Server Error)`。

**排查**：
1. 第一反应是 CORS 配置问题 —— 但配置明明有 `http://localhost:5173`
2. 注意细节：**500 才是重点**，CORS 错误是浏览器因为响应没有 CORS 头才报的
3. 手动测试 `hash_password`：
```
python -c "from passlib.context import CryptContext; ...print(pwd.hash('test123'))"
```
报错：`AttributeError: module 'bcrypt' has no attribute '__about__'`

**根因**：`bcrypt 5.0.0` 和 `passlib 1.7.4` 不兼容。bcrypt 5.x 删掉了 `__about__` 属性，passlib 启动时读版本号失败，整个 bcrypt 后端不可用 → `hash_password` 崩溃 → 接口 500 → 全局异常处理器返回 500 响应但没有 CORS 头 → 浏览器报 CORS 错误。

**修复**：
```
uv pip install "bcrypt==4.1.3"
```

**学习点（重要）**：
- **CORS 报错有时是烟雾弹**：真正问题是接口 500。浏览器对 500 响应不附 CORS 头，所以"看起来"是 CORS 问题。排查顺序：先看状态码，再看 CORS
- **依赖兼容性**：passlib 是 2020 年的库，bcrypt 新版 API 变了。遇到"库突然不能用"先怀疑版本
- **验证密码库是否可用**：直接用 python 命令行调一次哈希函数

---

## 九、关键技术点总结

### 9.1 FastAPI 请求→响应全链路

```
请求进来
  → CORSMiddleware（跨域）
  → 路由匹配（/api/v1/xxx）
  → 依赖注入解析（get_db、get_current_user）
  → 参数校验（请求体按 Schema 验证）
  → 业务逻辑执行
  → response_model 响应校验/序列化
  → 返回给前端
```

### 9.2 JWT 认证全链路

```
注册/登录
  → bcrypt 哈希/验密
  → 签发 JWT（payload 含 user_id + 过期时间，SECRET_KEY 签名）
  → 前端存 localStorage + 注入 axios 请求头

后续请求
  → 前端自动带 Authorization: Bearer xxx
  → get_current_user 解 token → 查库 → 返回用户
  → 接口直接用 current_user，无需再验证
```

### 9.3 前端数据流

```
useAuth 模块级单例（token/user 状态）
  → localStorage 持久化
  → axios 请求头注入
  → 各页面按 isLoggedIn 渲染
  → 登出清空所有状态
```

### 9.4 踩坑清单（速查）

| 坑 | 教训 |
|---|---|
| pyjwt vs python-jose | 先看 import 再装依赖 |
| schema 拼写/类型 | 对照模型逐字段核对 |
| 解包路径 | 先看后端 JSON 再写提取代码 |
| Transition 多根节点 | 组件必须单根元素 |
| 路由组件不刷新 | `<component>` 加 `:key` |
| CORS 报错 | 先查状态码，可能是 500 的烟雾弹 |
| bcrypt/passlib 版本 | 兼容性验证：命令行直接跑哈希 |
| 全局 UI 放条件分支 | 返回按钮要放分支外 |

---

## 十、遗留事项 / 上线前必做

1. **配置 `JWT_SECRET_KEY`**：`.env` 里设置强随机密钥（当前是开发兜底值，公开仓库里任何人都能看到 → 能伪造 token）
2. **`pyproject.toml` 缺少新依赖（重要）**：当前依赖清单只有 alembic/asyncmy/fastapi 等基础包，**没有 passlib、bcrypt、python-jose**。这就是 Bug 1、Bug 2（ModuleNotFoundError）的根源。修复后必须在 `backend/pyproject.toml` 的 `dependencies` 里补充并锁定版本，否则换台机器/重建环境会再次踩坑：
   ```toml
   "passlib>=1.7.4",
   "bcrypt==4.1.3",   # 锁定 4.x！5.x 与 passlib 不兼容（Bug 7）
   "python-jose[cryptography]>=3.5.0",
   ```
3. **`created_at` 一致性**：`RecommendationResponse` 里的 `created_at` 用 ISO 字符串，前端 `timeAgo()` 依赖它
4. **当前改动未提交**：所有改动还在工作区，建议功能稳定后 `git commit` 保存里程碑
5. **数据库表结构**：`users`/`comments`/`comment_likes` 表需要确认已在 MySQL 中建好（当前通过 seed 初始化了 MBTI 类型，用户/评论表依赖手工建表或后续迁移方案）

---

*文档生成时间：2026-07-31。开发仍在进行中，后续阶段会持续补充。*
