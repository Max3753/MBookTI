# MBookTI 开发纪实

> 一个人从零写的一个 MBTI 书籍推荐应用。前端 Vue 3 + 后端 FastAPI + MySQL，按自己的节奏迭代。
> 这篇记录主要给自己看：当时为什么这么写、踩了什么坑、后来怎么想的。配合 git 历史看效果更好。

---

## 七月：把项目从零搭起来

### 起步

项目定位很简单：根据 MBTI 人格类型推荐书。前后端分离，Vue 3 + FastAPI，数据库 MySQL，AI 用 DeepSeek。认证用 JWT，密码 bcrypt。

一开始没啥规划，就是想到什么加什么，很多设计是边写边改出来的。

### 主题切换不生效

第一件小事就卡住了。主题按钮点了没反应，查了半天发现是 Tailwind v4 的坑——v4 默认不再自动响应 `.dark` 类，必须手动声明：

```css
@custom-variant dark (&:where(.dark, .dark *));
```

v3 和 v4 的暗色模式机制完全不一样，这个以后换版本要记得。

### 删掉语言切换

本来做了中英切换，后来觉得没必要，砍了。改成纯静态对象导出 `t`，不用 `ref`。教训是：不需要响应式的东西别塞进 `ref`，纯常量直接导出就行。

### 安全审查（第一次推 GitHub 前）

- 根目录建 `.gitignore`：`.env`、`node_modules`、`dist`、`__pycache__` 全加进去
- `config.py` 去掉数据库 URL 硬编码
- `git add -n --all` 预览确认没有敏感文件

推送前先自查敏感信息，这个习惯后面一直保持着。

### Schemas 重构

一开始接口直接返回裸 dict，FastAPI 不知道响应结构，全靠前端猜。后来统一加了 Pydantic Schema 层：

```python
class ApiResponse(BaseModel, Generic[T]):
    data: T
    message: str = "success"

class ApiListResponse(BaseModel, Generic[T]):
    data: list[T]
    total: int
    message: str = "success"
```

好处是接口有了明确的请求/响应结构，`response_model=` 会做响应校验，字段缺失直接 500，很多 bug 被拦在出口。后面几次拼写错误、类型不匹配都是靠它暴露的。

响应统一成 `{data, message}`，前端解包逻辑也统一了。

### JWT 认证

核心就三个文件：`password.py`（bcrypt 哈希）、`jwt.py`（签发/验证）、`deps.py`（依赖注入）。

最有价值的其实是 FastAPI 的依赖注入——任何接口想"要求登录"，只要：

```python
async def create_comment(..., current_user: User = Depends(get_current_user)):
```

不用每个接口重复写验证代码，FastAPI 自动完成取 token → 解析 → 查库 → 给用户对象。

设计决策：注册成功直接返回 token（注册即登录），省去前端二次跳转。

### 评论系统

评论 + 点赞。点赞的 toggle 逻辑挺妙的：

```python
existing = await session.execute(select(CommentLike).where(...))
if like:
    await session.delete(like)  # 有记录 → 取消赞
else:
    session.add(...)            # 没记录 → 点赞
```

`comment_likes` 是联合主键 `(user_id, comment_id)`，天然防重复点赞。一个接口搞定两种行为，前端按钮不用维护状态。

### 前端登录态

`useAuth` 做成**模块级单例**——`ref` 定义在函数外面，所有组件拿到同一份状态：

```ts
const token = ref(localStorage.getItem('token') || '')

export function useAuth() {
    // 所有组件调用都共享上面的 token
}
```

刷新页面 token 还在（localStorage），模块加载时自动恢复 axios 请求头 `Authorization: Bearer xxx`。

路由守卫：已登录还访问登录页 → 踢回首页。

### 联调阶段：7 个 Bug

这一段是重点，按踩坑顺序记。

**Bug 1 & 2：后端起不来，ModuleNotFoundError**
装 passlib 报错，装了又报 jose。根因：`pyjwt` 和 `python-jose` 是两个不同的库，代码里 `from jose import jwt`，我装的是 `pyjwt`。教训：**先看代码怎么 import 再装包**。

**Bug 3：MBTI 接口 500，32 个 ResponseValidationError**
Schema 里 `nane_en`（拼写错了，应是 `name_en`），还有 `traits` 声明成 dict 但库里存的是数组。`response_model` 校验直接爆出来。教训：Schema 是契约，必须和模型/数据实际形态一致。

**Bug 4：详情页空白，后端却 200 OK**
后端返回 `{data: {items: [...], mbti_type: {...}}}`，前端写 `recRes.data || []` 直接遍历对象 → 空白。教训：**解包前先看后端 JSON 长什么样**，F12 Network 看响应体再写代码。

**Bug 5：返回按钮不显示**
放进了 `v-else` 分支里，报错时就不渲染。修正：移到所有条件分支外。教训：全局可见的 UI 不能放条件渲染分支里。

**Bug 6：导航后页面空白，刷新才恢复**
Vue 警告提示：`Component inside <Transition> renders non-element root node`。两个问题叠加：
1. 组件有多个根节点，`<Transition>` 只支持单根元素 → 组件渲染中断
2. `<component :is>` 缺 `:key`，路由切换时组件被复用，`onMounted` 不执行

修复：组件包一层 `<div>` + `:key="$route.fullPath"`。

**Bug 7：注册报 CORS 错误，根因是 bcrypt**
表面是 CORS，实际是接口 500（浏览器对 500 响应不附 CORS 头）。手动跑哈希发现：`bcrypt 5.0.0` 和 `passlib 1.7.4` 不兼容，bcrypt 5.x 删了 `__about__` 属性，passlib 启动就崩。修复：锁 `bcrypt==4.1.3`。

教训（重要）：**CORS 报错有时是烟雾弹，先看状态码**；passlib 是 2020 年的库，跟新版本库混用容易翻车。

---

## 八月：功能爆炸式增长

### 8/1 部署 + 后台 + 个人中心

- Docker 部署：MySQL + FastAPI + Nginx 三容器编排。端口冲突改映射 3307/8080（本地已有 3306/80）
- 数据库迁移脚本化：本地导入 Docker 容器的脚本、云服务器间迁移脚本
- 后台：通知/公告/管理后台/个人中心/忘记密码全做出来了
- 顺手修了几个安全问题：密码重置泄露、AI 生成接口滥用（限流）
- 加了 README

### 8/2 视觉重设计 + MBTI 测试

全站改成报纸印刷风格（Newsprint），保留双模式主题。新增直觉型 MBTI 测试功能（测试/结果/保存）。类型图片挪到 `resources/mbti_img/` 统一管理。

### 8/3 阅读器来了

开始做电子书阅读器，支持 EPUB/TXT，在线阅读 + 进度保存。入口放在个人主页。

同一天修了一堆体验问题：
- 时间显示 8 小时偏差（时区没对齐）
- 头像上传（跨设备不同步也修了）
- 移动端用户名显示优化
- 保存资料后其他页面不同步

### 8/4 阅读器大改 + PDF

- 支持 PDF（pdf.js），加页面缩放
- 阅读器整体重做：报纸排版、目录抽屉、进度条、字体调节、空态引导
- 进度云端同步重构：本地文件 + SHA-256 内容关联（文件变了就找不到进度）
- 踩坑：nginx 缺 `.mjs` MIME 映射导致 pdf.js worker 加载失败，得在 location 级加映射（全局加会覆盖其他 MIME）
- 踩坑：http 环境 `crypto.subtle` 不可用，SHA-256 加纯 JS 实现兜底
- 离开阅读器时清理 adapter 内存态，否则 SPA 跳回主页会显示空态

### 8/5 社交功能 + 翻页地狱

- 社交：公开用户主页、书籍星级评分、评论回复（可折叠，默认折叠，回复后自动展开）、书友动态流、关注系统、主页隐私开关
- 阅读器翻页改造：热区/键盘/滑动翻页、章节位置显示、排版设置（字体/行距/边距/缩进）、翻页转场、批量导入

翻页这块踩的坑最多，单独记：
1. 点击热区和 EPUB 目录/PDF 滚动冲突 → EPUB 改 iframe 内监听，热区仅限 TXT
2. 统一点击分区规则：左 1/3 上一页、右 2/3 下一页，去掉中央死区
3. 连点翻页来回跳 → 加 250ms 翻页锁 + 位移判定
4. 还是跳 → 翻页改为 promise 完成态 + 最短间隔双锁

### 8/6 打磨 + 素材模式

- 点击分区翻页：桌面端保留，移动端只留滑动手势
- 点击分区改为容器坐标监听（去掉覆盖层热区），恢复正文文字选取（拖选/双击选词不翻页，单击按分区翻页）
- EPUB 章节名显示修复：spine 条数(72) ≠ 目录顶层项数(9)，改用 spine href 的 part 基名匹配目录项
- 管理员素材模式：仅管理员开启后去除灰度滤镜，配图/封面恢复全彩，方便截图导出自媒体素材
- AI 推荐文案优化：换书分支按人格偏好体裁选书并生成关联文案，AI 理由强化三要素，书卡新增推荐理由标签/书籍简介/用户评分

### 8/7–8/9 AI 推荐强化

- AI 推荐改为逐本独立生成 + 推荐词统一生成，调用优化、降级可感知（AI 挂了用户能看到提示而不是傻等）
- `relevance_score` 改为 AI 评分 + 缓存，跨批次恒定（以前每次生成可能不同）

### 8/10 搜索 + 去重（今天）

- 新增站内搜索：`/books/search` 接口（书名/作者模糊 + ISBN 精确 + 分页），前端报头搜索框 + 结果页防抖输入即搜 + 加载更多，书籍卡片组件化（收藏页复用）
- 书籍详情页封面去除灰度滤镜，所有入口都显示全彩
- 收藏页 BookCard 类型错误修复（Docker 构建 TS2739）——收藏数据改用 `BookSummary` 类型对齐后端
- 搜索结果重复：去重键改为书名主体提取，消除作者全称/简称与副标题版本变体导致的重复

#### 书籍防重复（压轴）

发现一个深坑：**isbn 由 AI 生成但从未写入，100% 为 NULL**——isbn 唯一约束空转，同名同作者的书能无限入库。分三层修：

1. **数据库层**：`books` 表加 `UNIQUE(title, author)` 约束 + 幂等迁移脚本。脚本先清理既有重复（保留最小 id，外键引用重定向），再加约束。加了个健壮性处理：被删书和保留书若被同一用户/类型引用（比如推荐给同一 MBTI、被同一用户收藏），直接 UPDATE 会触发别的表唯一约束冲突，得先删冲突行再重定向。这个场景用临时库完整模拟过才敢上线。
2. **AI 推荐写库**：`_upsert_book` 内存快照未命中时加 DB 兜底查重，嵌套事务捕获 UNIQUE 冲突回查复用。
3. **前端搜索防抖**：`router-view` 的 key 改 `route.path`，query 变化不再重建组件触发重复搜索。

---

## 踩坑清单（速查）

| 坑 | 教训 |
|---|---|
| pyjwt vs python-jose | 先看代码怎么 import 再装包 |
| Schema 拼写/类型 | 对照模型逐字段核对，靠 response_model 校验兜底 |
| 解包路径 | 先看后端 JSON 再写提取代码 |
| Transition 多根节点 | 组件必须单根元素 |
| 路由组件不刷新 | `<component>` 加 `:key` |
| CORS 报错 | 先看状态码，可能是 500 的烟雾弹 |
| bcrypt/passlib 版本 | 锁版本，命令行直接跑哈希验证 |
| 全局 UI 放条件分支 | 返回按钮要放分支外 |
| Tailwind v4 暗色 | 需手动声明 `@custom-variant dark` |
| nginx 缺 MIME | location 级补 `.mjs` 映射 |
| isbn 唯一约束空转 | AI 生成的字段可能永远为空，去重键要选真实存在的列 |
| 迁移脚本外键冲突 | 先删冲突行再重定向，别直接 UPDATE |
| mysql:8 浮动标签 | 锁具体大版本（8.4），版本漂移会毁数据卷 |
| 服务器 apt 源 | 国内服务器换阿里云源，deb.debian.org 连不上会构建超时 |
| 删数据卷前 | 必须先确认备份/导出，再执行破坏性操作 |

---

## 九月：服务器部署排障（一次差点翻车的经历）

### 起因

服务器上改了 Docker 镜像源配置，重启了 docker，所有容器关闭。重新 `up -d --build` 后，**卡了 4450 秒（74 分钟）**。

### 排查过程

1. **`ps -a` 一看**：backend `Exited (1)`，mysql `Exited (0)`——不是卡住，是 backend 无限重启失败
2. **backend 日志**：`JWT_SECRET_KEY 未配置或仍为默认值`——环境变量是空的
3. **但 `compose config` 能读到值**！说明 `.env.docker` 没问题，是**容器是旧的没被重建**——`up -d` 对已存在容器只启动不重建，旧容器带着空环境变量无限重启
4. **`--force-recreate` 强制重建后**，构建又卡住——`apt-get` 连不上 `deb.debian.org`（国内服务器访问 Debian 官方源超时）
5. **Dockerfile 换阿里云源**后构建通过，MySQL 又起不来

### MySQL 版本漂移（最深的坑）

```
Cannot boot server version 80027 on data directory built by version 80411
```

数据卷是 **8.4.11** 建的，但 `mysql:8` 浮动标签这次拉到了 **8.0.27**——版本降级，8.0 读不了 8.4 的数据目录。改成 `mysql:8.4` 后又报：

```
This redo log was created with MySQL 8.0.27, and it appears logically non empty
```

8.0.27 尝试启动时非正常关闭，留下了 8.4 无法升级的 redo log。**数据卷彻底损坏，两边都起不来**。

最终只能删数据卷重建。**教训：`mysql:8` 这种浮动标签必须锁死具体版本**，否则版本漂移会毁掉数据卷。

### 我的失误

删数据卷前，我只标注了"⚠️ 数据会丢"，没有主动让用户先备份/导出。虽然当时 MySQL 已起不来（常规 mysqldump 已不可能），但至少应该提 `innodb_force_recovery` 抢救选项，并确认备份存在后再删。**以后破坏性操作流程：先确认备份 → 给导出命令 → 备份成功 → 才执行删除**。

### 修复落地

- `backend/Dockerfile`：apt 换阿里云源（`sed` 替换 deb.debian.org → mirrors.aliyun.com）
- `docker-compose.yml`：`mysql:8` → `mysql:8.4`（锁版本）
- 两个改动已提交推送，下次部署 `git pull` 即可，不会再踩这两个坑

### 数据影响

服务器 MySQL 数据卷清空重建（用户数据丢失），但影响不大——云服务器已开启文件备份服务兜底，且本地有 `mbookti_dump.sql` 可恢复。

---

## 还没做 / 想做的

- `.env.docker` 里 JWT_SECRET_KEY 已配置，但要确保所有环境都用强随机值
- `created_at` 序列化一致性再核对一遍
- Docker 部署的服务器端验证清单已写（`docs/server-deploy-checklist.md`，未入库）
- 数据库表结构变更的迁移流程可以更规范（现在靠幂等脚本手动跑）

*持续更新中。*
